{
  "buildCommand": "expo export -p web && node ./scripts/prepare-web-files.js",
  "outputDirectory": "dist/client",
  "devCommand": "expo",
  "cleanUrls": true,
  "framework": null,
  "rewrites": [
    {
      "source": "/:path*",
      "destination": "/"
    }
  ]
}
